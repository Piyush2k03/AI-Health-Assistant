apiVersion: v1
kind: Service
metadata:
  name: ai-health-assistant-service
  labels:
    app: ai-health-assistant
spec:
  type: NodePort          # or ClusterIP, depending on college instructions
  selector:
    app: ai-health-assistant
  ports:
    - port: 80
      targetPort: 8501
      nodePort: 30051     # only if type: NodePort & allowed by college
